# proxyshop-custom-json
A Proxyshop plugin that sources card data from a local JSON file instead of (or layered on top of) the Scryfall API — for custom proxies, offline rendering, and translated/overridden cards.
